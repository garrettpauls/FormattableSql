<Window x:Class="SimpleNotes.Views.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleNotes.Views"
        mc:Ignorable="d"
        Title="Shell" Height="300" Width="300">
  <Window.DataContext>
    <local:ShellViewModel />
  </Window.DataContext>
  <Grid Margin="4">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <DockPanel LastChildFill="True">
      <TextBlock Text="Search: " DockPanel.Dock="Left" />
      <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
    </DockPanel>

    <ListView Grid.Row="1" ItemsSource="{Binding Notes}" SelectionMode="Single" Margin="0 4 0 0">
      <ListView.ItemsPanel>
        <ItemsPanelTemplate>
          <VirtualizingStackPanel />
        </ItemsPanelTemplate>
      </ListView.ItemsPanel>
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Model.Title}" />
          <GridViewColumn Header="User" DisplayMemberBinding="{Binding Model.User}" />
        </GridView>
      </ListView.View>
    </ListView>
  </Grid>
</Window>
